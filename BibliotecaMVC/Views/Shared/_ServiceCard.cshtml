@model BibliotecaMVC.Models.ServiceCard

<div class="card-demo">
    <img src="@Model.ImageUrl" class="card-img-top" alt="Catálogo de libros">
    <div class="card-body">
        <p class="card-text">
            @Model.Text
        </p>
    </div>
</div>